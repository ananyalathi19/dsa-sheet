#include <bits/stdc++.h>

using namespace std;

int main()
{
    int a,b,sum=0;
    cout<<"enter the no.s";
    cin>>a>>b;
    for(int i=a;i<=b;i++)
    {
        sum=sum+i;
    }
    cout<<sum;

    return 0;
}
